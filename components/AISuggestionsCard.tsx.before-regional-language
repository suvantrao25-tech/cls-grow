"use client";

import { useBusiness } from "./BusinessContext";

export default function AISuggestionsCard() {
  const { audit } = useBusiness();

  const nextMove =
    audit.suggestions[0] ||
    audit.tasks[0] ||
    "Complete your business profile to identify your next growth move.";

  const whyItMatters =
    audit.weaknesses[0] ||
    "Taking one focused action each week can help improve your business.";

  return (
    <div className="bg-white rounded-2xl shadow-sm border border-blue-100 p-6 mt-8">
      <p className="text-sm font-semibold text-blue-600 uppercase tracking-wide">
        Next Growth Move
      </p>

      <h2 className="text-2xl font-bold text-gray-900 mt-2">
        What should you do next?
      </h2>

      <div className="mt-5 bg-blue-50 border border-blue-100 rounded-xl p-5">
        <p className="text-lg font-semibold text-gray-900">
          {nextMove}
        </p>

        <div className="mt-4">
          <p className="text-sm font-semibold text-gray-700">
            Why this matters
          </p>

          <p className="text-sm text-gray-600 mt-1">
            {whyItMatters}
          </p>
        </div>
      </div>

      <div className="mt-5">
        <p className="text-sm text-gray-500">
          CLS GROW focuses on one practical growth action at a time.
        </p>
      </div>
    </div>
  );
}
